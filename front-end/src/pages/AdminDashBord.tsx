import React from "react";
import { Outlet } from "react-router-dom";

const AdminDashBord = () => {
    return (
        <div>
            AdminDashBord
            <Outlet />
        </div>
    );
};

export default AdminDashBord;
